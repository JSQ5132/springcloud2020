server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #配置Nacos服务注册中心地址
      config:
        server-addr: localhost:8848 #配置Nacos服务注册中心地址
        file-extension: yaml #指定yaml格式的配置
        namespace: f4e7128c-0518-4970-8fcd-027a646619f6 #指定命名空间
        group: DEV_GROUP #指定分组
